# React Boilerplate 

Run this to build
```npm run build ```

Run this to start dev server
```npm run dev-server ```

Run this to build development compile with ```style-loader```
```npm run build ```